# 安全策略

## 支持范围

Project Brain 尚处于 `0.x` 阶段。安全修复只保证进入最新发布版本；旧版本可能要求先升级。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。报告应包含：

- 受影响版本、操作系统和 Agent adapter；
- 最小复现步骤与预期/实际行为；
- 是否能绕过 hard rule、跨越 `project_key` 边界、篡改审计或覆盖用户 Hook；
- 如已知，建议的缓解方式。

不要在公开 issue 中披露可直接利用的细节。如果私密报告入口不可用，可只创建不含漏洞细节的公开 issue，请求维护者提供私密联系渠道。

收到报告后，维护者应先确认影响范围，再协调修复、回归测试和披露时间。Project Brain 不承诺固定响应 SLA，但会优先处理可能导致规则绕过、跨项目数据泄漏或任意命令执行的问题。

## 威胁边界

- Project Brain 只信任仓库内显式规则和已接受决策；LLM 推断不能成为 hard veto。
- Codex 对非托管命令 Hook 的信任由 Codex 自身管理，`doctor` 不能伪造或自动批准该状态。
- 本机管理员、能够替换已安装二进制的用户，以及被信任 Hook 所调用的外部工具位于本工具的信任边界之外。
